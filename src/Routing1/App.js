import React, { Component } from 'react';

class App extends Component {
    render() {
        return (
            <div className='mt-3' style={{ backgroundImage: url("gradient_bg.png") }}>
                <i className='float-right p-1'
                    style={{ borderRadius: '10px', backgroundColor: '#add8e6a1' }}>
                    const randumNumber = math.floor(math.random()*this.state.options.length)
                    </i>
            </div>
        )
    }
}

export default App;
